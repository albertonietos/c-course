#include "source.h"

#include <stdio.h>
#include <math.h>

void simple_multiply(void)
{
    int a, b, ret;
    ret = scanf("%d %d", &a, &b);
    if (ret > 0) {
        int c = a * b;
        printf("%d * %d = %d\n", a, b, c);
    }
}


void simple_math(void)
{
    
}




